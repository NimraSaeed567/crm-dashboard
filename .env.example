VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Server-side only (Vercel serverless function /api/ask) — do NOT prefix with VITE_,
# that would bundle it into client-side JS and expose it publicly.
OPENROUTER_API_KEY=
